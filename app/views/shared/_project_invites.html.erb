<% if project_invites.count > 0 %>
    <% project_invites.each do |project| %>
        <li><div align="center"><%= link_to project.name, edit_project_path(project) %></div></li>
    <% end %>
<% else %>
    <li>
      <div align="center">You have no invites.</div>
    </li>
<% end %>