<% if target && target.errors.any? %>
    <div class="error">
      <%= pluralize(target.errors.count, "error") %> prohibited this task from being saved:
      <% target.errors.full_messages.each do |msg| %>
          <div class="error_message"><%= msg %></div>
      <% end %>
    </div>
    <br/>
<% end %>