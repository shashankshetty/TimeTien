<!DOCTYPE html>
<html>
<head>
  <title>TimeZen - <%= yield(:title) %></title>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application" %>
  <%= javascript_include_tag "application" %>
  <%= favicon_link_tag 'favicon.ico' %>
</head>
<body>
<div class="navbar">
  <a class="brand" href="<%= root_url %>">
    <span class="app-name">Time Zen</span>
  </a>

  <div class="navbar-inner">
    <div class="container" style="width:100%">
      <% if user_signed_in? %>
          <% group_invites = current_user.nil? ? [] : current_user.get_group_invites %>
          <% user_groups = current_user.nil? ? [] : current_user.get_user_groups %>
          <ul class="nav pull-right">
            <li class="active"><%= link_to 'add old tasks', new_task_path %></li>
            <li class="active"><%= link_to 'analyze tasks', analyze_user_tasks_path %></li>
            <li class="active"><%= link_to 'manage tags', tags_path %></li>
            <li class="dropdown" id="menu1">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#menu1">
                manage groups
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <%= render "shared/groups", :user_groups => user_groups %>
              </ul>
            </li>
            <li class="dropdown" id="menu2">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#menu2">
                group invites&nbsp;<span id="invite"><%= group_invites.count %></span>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <%= render "shared/group_invites", :group_invites => group_invites %>
              </ul>
            </li>
            <li class="divider-vertical"></li>
            <li class="dropdown" id="menu3">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#menu3">
                <%= image_tag avatar_url(current_user), :class => "gravatar" %>&nbsp;<%= current_user.display_name %>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "user settings", edit_user_registration_path, :id => 'user_settings', :class => "intro" %></li>
                <li><%= link_to "sign out", destroy_user_session_path, :method => :delete %></li>
              </ul>
            </li>
          </ul>
      <% else %>
          <ul class="nav pull-right">
            <li class="active"><%= link_to 'about', about_path %></li>
          </ul>
      <% end %>
    </div>
  </div>
</div>
<div id="main" class="container">
  <div class="title"><%= yield(:title) %>
    <hr/>
  </div>
  <% flash.each do |key, value| %>
      <% if key.to_s != "error" && !value.blank? %>
          <div class="<%= key %>"><%= value %></div>
      <% end %>
  <% end %>
  <% if !(flash[:info].nil? && flash[:notice].nil? && flash[:success].nil? && flash[:error].nil?) %>
      <br/>
  <% end %>
  <%= yield %>
</div>
<% if !user_signed_in? %>
    <div id="footer" class="ui-tabs-nav ui-widget-header">© 2012 TimeZen</div>
<% end %>
</body>
</html>